<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置背景色图像资源 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" />
        </shape>
    </item>
    <!-- 设置第二级进度条颜色图像资源 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
            </shape>
        </clip>
    </item>
    <!-- 设置第一级进度条颜色图像资源 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <gradient
                    android:angle="0"
                    android:startColor="@color/webview_progressbar_start_color"
                    android:centerColor="@color/webview_progressbar_center_color"
                    android:endColor="@color/webview_progressbar_end_color" />
            </shape>
        </clip>
    </item>
</layer-list>