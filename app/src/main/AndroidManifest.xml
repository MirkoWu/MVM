<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mirkowu.mvm">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".MVMApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:preserveLegacyExternalStorage="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.websocket.WebSocketActivity"></activity>
        <activity android:name=".aidl.client.ClientActivity" />

        <service
            android:name=".aidl.server.MyService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.mirkowu.mvm.aidl.IMyAidlInterface" />
            </intent-filter>
        </service>

        <activity android:name=".test.TestActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.HandlerActivity" />
        <activity android:name=".ui.BitmapActivity" />

        <service
            android:name=".service.MyService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".ui.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.widgetdemo.WidgetDemoActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="sensor" />
        <activity android:name=".ui.download.DownloadActivity" />
        <activity android:name=".ui.imagepicker.ImagePickerActivity" />
        <activity
            android:name=".ui.webview.WebActivity"
            android:launchMode="singleTask"
            android:process="@string/webview_process"
            android:screenOrientation="portrait"
            android:theme="@style/WebViewTheme" />
        <activity android:name=".ui.recycelerview.GridListActivity" />
        <activity android:name=".ui.recycelerview.LinearListActivity" />
        <activity android:name=".ui.mvvm.viewbinding.DataBindingActivity" />
        <activity android:name=".ui.mvc.MVCActivity" />
        <activity android:name=".ui.mvp.MVPActivity" />
        <activity
            android:name=".ui.mvvm.MVVMActivity"
            android:launchMode="singleTask" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.app.file_provider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="resource" />
        </provider>

        <service android:name=".viewbinding.RestartAppService" />
        <service android:name=".service.MyIntentService" />
    </application>

</manifest>